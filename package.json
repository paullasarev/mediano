{
  "name": "mediano",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "preinstall": "grunt --version && bower --version && karma --version && echo done preinstall || npm install -g grunt-cli bower karma-cli"
  },
  "dependencies": {
    "express": "~3",
    "hjs": "*"
  },
  "devDependencies": {
    "browserify": "*",
    "debowerify": "*",
    "grunt": "*",
    "grunt-browserify": "*",
    "grunt-contrib-copy": "*",
    "grunt-contrib-clean": "*",
    "grunt-contrib-watch": "*",
    "grunt-nodemon": "*",
    "grunt-concurrent": "*",
    "grunt-contrib-less": "*",
    "karma": "^0.12.1",
    "karma-jasmine": "^0.1.5",
    "karma-chrome-launcher": "^0.1.2"
  },
  "//": [
    "preinstall: npm install -g grunt-cli bower karma-cli",
    "postinstall: bower install"
  ]
}
