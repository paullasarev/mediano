{
  "name": "mediano",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "prestart": "gulp",
    "test": "gulp test",
    "preinstall": "gulp --version && bower --version && karma --version && mocha --version && echo done preinstall || npm install -g gulp bower karma-cli mocha",
    "postinstall": "bower install"
  },
  "dependencies": {
    "express": "~3",
    "hjs": "*",
    "restify": "^2.8.2",
    "mocha": "^1.21.4",
    "yargs": "^1.3.1",
    "lodash-node": "^2.4.1"
  },
  "devDependencies": {
    "browserify": "*",
    "debowerify": "*",
    "karma": "^0.12.1",
    "karma-jasmine": "^0.1.5",
    "karma-chrome-launcher": "^0.1.2",
    "yargs": "^1.2.1",
    "run-sequence": "^0.3.6",
    "gulp-browserify": "^0.5.0",
    "gulp-clean": "^0.2.4",
    "gulp-livereload": "^1.3.1",
    "gulp": "^3.6.0",
    "gulp-less": "^1.2.3",
    "cheerio": "^0.15.0",
    "gulp-mocha": "^0.4.1",
    "chai": "^1.9.1",
    "gulp-karma": "0.0.4",
    "marked": "^0.3.2",
    "highlight.js": "^8.0.0"
  }
}
